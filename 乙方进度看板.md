# 乙方交付看板（逻辑 / 数据 / 后端）

> 更新：2026-09-13 · 乙方 = `lib/game/config/` `lib/game/engine/` `lib/game/state/` `server/` `public/images` `public/audio`
> 自检命令：`npm run verify`（143 项）· `npm run verify:engines`（82 项）· `npm run typecheck`

---

## 一、任务进度

| 编号 | 内容 | 状态 | 交付物 | 验证方式 |
|---|---|---|---|---|
| B1 | 地图与关卡（200 关 / 28 怪物） | ✅ 完成 | `lib/game/config/maps.ts` | 143 项自检第 1 节；4 图 × 50 关、8 Boss、挂载表逐条断言 |
| B2 | 配置统一出口 | ✅ 完成 | `lib/game/config/index.ts` | 单点 `export *`，`tsc` 通过 |
| B3 | Zustand 存档状态机 | ✅ 完成 | `lib/game/state/store.ts` `defaults.ts` | 自检第 6、7 节：迁移往返 + 边界 2 全部动作 37 项 |
| B4 | 派生数据（战力 / 属性 / Build 评分） | ✅ 完成 | `lib/game/state/selectors.ts` | 自检第 8 节 16 项 |
| B5 | 离线挂机（24h 上限） | ✅ 完成 | `lib/game/engine/idle.ts` | 自检第 3 节：48h ≈ 24h、0s 零收益 |
| B6 | 境界突破（失败保护） | ✅ 完成 | `lib/game/engine/breakthrough.ts` | 自检第 4 节：不掉境界 / 不掉装备 / 修为不清零 |
| B7 | 掉落与背包（保底 / 自动分解 / 扩容） | ✅ 完成 | `lib/game/engine/loot.ts` | 自检第 2 节：蓝 ≤7、紫 ≤26、容量上限、自动分解 |
| B8 | 扫荡 | ✅ 完成 | `lib/game/engine/battle.ts` | 自检第 5 节：simulate 与 LiveBattle 胜率 / 伤害一致 |
| B9 | NPC 立绘（13 人 × 五态） | 🔄 进行中 | `public/images/npc/` | 13 张基础立绘已落盘；五态已备 5/52，缺的自动兜底 `<id>.png` |
| B10 | 地图背景与 Boss 立绘 | ✅ 完成 | `public/images/` | 4 张地图专属背景 + 玩家立绘；自检第 9 节断言落盘 |
| B11 | 音效素材 | 🔄 进行中 | `public/audio/` | 目录已建，素材待补 |
| B12 | 云存档后端 | ✅ 完成 | `server/` | `cd server && python -m pytest tests -q` → 31 passed |
| B13 | 多端登录（游客 / 微信 / QQ / 邮箱） | ✅ 完成 | `server/app/routers/auth.py` | 同上，含游客绑定不丢档 |
| B14 | 埋点（PRD 49 章 20 项） | ✅ 完成 | `server/app/routers/analytics.py` | 同上 |

## 二、验收标准对照（分工说明.md 第七节 · 乙）

| 标准 | 结论 | 证据 |
|---|---|---|
| 200 关逐关可查，无连续 20 关纯加血 | ✅ | 自检：最长连续纯普通关 **4 关**（节奏 1~4 普通 / 5 精英 / 10 小 Boss） |
| `simulate()` 与 `LiveBattle` 胜率偏差 < 5% | ✅ | 自检第 5 节：总胜率偏差 < 1%，输出 / 承伤偏差 < 20% |
| 存档刷新不丢；version 升级有迁移；导出 / 导入可用 | ✅ | 自检第 6 节：迁移往返 + 脏数据不崩 |
| 24h 离线上限正确；离线不替玩家做重大选择 | ✅ | 自检第 3 节：48h 收益 = 24h 收益 |
| 突破失败不掉境界 / 不掉装备 / 修为不清零 | ✅ | 自检第 4 节 |
| 前 10 分钟必见蓝装；30 分钟内至少一次紫装 | ✅ | 保底计数：蓝 ≤6 次掉落、紫 ≤25 次掉落（软保底，非硬卡） |
| 每个 Boss 至少 2~3 种合理应对方式，不存在唯一解 | ✅ | 自检第 5b 节：8 个 Boss 均 ≥2 种**真实生效**的机制 |
| 剧情 flag 可重放；NPC 死亡不无条件出现；唯一物品不可重复获得 | ✅ | 自检第 7 节：`submitChoice` 写 flag / 因果 / seenNodes；秘境收益单次结算 |

## 三、给甲方的接口说明（改动 `lib/game/types.ts` 之外的契约）

1. **`map.bg` 已切到专属背景**：`/images/bg-qingshi-village.png`、`bg-heifeng-ridge.png`、`bg-yunze-market.png`、`bg-luoxia-mountain.png`。
2. **Boss 立绘映射**：`components/game/battle/useBattleDriver.ts` 里的 `portraits` 表仍指向旧的 `boss_placeholder_shanjun`，未知 id 会兜底到 `boss-black-wolf.png`，**不会破图**。8 个新 Boss id 见 `MONSTERS`（`boss_shanjun` / `boss_yushou_jiang` / `boss_luoqi` / `boss_heifeng_daoren` / `boss_tu_san` / `boss_guchangfeng` / `boss_yinshan_gulong` / `boss_xie_wuchen`）。
3. **新增战斗事件语义**（复用既有 `cast` 类型，UI 无需改代码即可显示）：
   - `法宝封印`：敌方周期性封住主动 / 被动法宝 6s（间隔 22s，首次 14s）→ 表现为该时段内玩家无法宝出手。
   - `形态变化`（`phase`）：Boss 血量低于 40% 后攻击力 ×1.25。
4. **`resist` 语义修正**：只减免**元素**伤害（五行），物理 / 神魂 / 真伤不受影响 —— 与 `LiveBattle.failReason()` 的提示一致。
5. **`monster.icon` 全部取自 `GameIcon` 注册表**，可直接渲染，无需新增图标。

## 四、待办 / 卡点

- **卡点（乙方侧）**：无。B1 已完成，B3 / B4 已解冻，甲方 F1 之后的接入不再被挡。
- **进行中**：B9 五态立绘补齐、B11 音效素材。
- **需甲方确认**：`components/game/**` 有 4 个 `tsc` 报错（`GameShell.tsx` 传 `onOpenIdle`、`BattleScreen.tsx` 三处类型不匹配），属甲方文件，乙方未改动。

## 五、关键设计决定（避免重复沟通）

- **关卡节奏**：每 10 关一个小 Boss（第 10/20/30/40 关），第 50 关为章节 Boss；精英在第 5/15/25/35/45 关。故最长连续纯普通关为 4 关。
- **曲线连续性**：后一张图的基准 = 前一张图基准 × `growth^50`，200 关 HP 曲线在换图处**无阶跃**。
- **倍率口径**：`MONSTERS` 里的 `hpMul/atkMul/defMul` 是「同档次内」相对值，与 `monsterStats()` 的 `kindMul`（普通 1 / 精英 1.6 / Boss 2.5）相乘。
- **剧情 / 秘境挂载**：全部走 `MapStage.storyId` / `realmId`，表见 `分工说明.md` B1，逐条被自检断言。
