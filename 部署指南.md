# 凡尘问道 · 部署指南

架构：一个 Vercel 项目（Services）+ Neon PostgreSQL。

```text
浏览器
  └─ 同一域名
      ├─ /api/*  → api 服务（FastAPI，server/，Python serverless function）
      └─ 其余路径 → web 服务（Next.js，仓库根目录）
api 服务 ──DATABASE_URL──> Neon PostgreSQL（表自动创建，启动时幂等 create_all）
```

## 一、Neon 数据库

已就绪（控制台创建项目即可拿到连接串）。应用启动时自动建表，无需手动执行
`server/sql/schema.sql`（该文件供 DBA 审查 / 手工建表用）。

## 二、Vercel 部署（Services 单项目，推荐）

1. Vercel → Add New Project → 导入本仓库。仓库根目录的 `vercel.json` 已声明两个服务：
   - `web`：根目录，Next.js
   - `api`：`server/` 目录，FastAPI，入口 `app.main:app`
2. Settings → Environment Variables 填入（值见私发 `.env`，**不要写进仓库**）：

   | 变量 | 说明 |
   | --- | --- |
   | `DATABASE_URL` | Neon 连接串（`postgresql://` 原样填即可，应用自动转 psycopg 方言） |
   | `JWT_SECRET` | 随机 64 位十六进制 |
   | `CREDENTIAL_PEPPER` | 随机 64 位十六进制（与 JWT_SECRET 不同） |
   | `API_PREFIX` | 必须为 `/api`（Services 按路径转发且保留原始路径） |

3. Deploy。验证：`https://<域名>/api/health` 返回 `{"status":"ok",...}`，
   打开首页进入游戏后左下角出现「已云同步」徽章。

前端默认同源请求 `/api/*`，无需 `NEXT_PUBLIC_API_URL`。

## 三、备选：拆成两个 Vercel 项目

若 Services 不可用（该能力可能受套餐限制）：

1. **后端项目**：导入同一仓库，Root Directory 设 `server/`。FastAPI 会被自动识别
   （`requirements.txt` + `app/main.py`），整个域名都是 API，**不要设 `API_PREFIX`**。
   环境变量：`DATABASE_URL` / `JWT_SECRET` / `CREDENTIAL_PEPPER`，
   外加 `CORS_ORIGINS=https://<前端域名>`。
2. **前端项目**：Root Directory 为仓库根，环境变量
   `NEXT_PUBLIC_API_URL=https://<后端域名>`。

## 四、本机联调

```bash
# 终端 1：后端（默认 SQLite；server/.env 里配了 Neon 则直连 Neon）
cd server && pip install -r requirements.txt
uvicorn app.main:app --reload --port 8000

# 终端 2：前端（next.config.mjs 已把 /api/* 代理到 127.0.0.1:8000）
npm install && npm run dev
```

自检：

```bash
cd server && python -m pytest tests -q   # 后端 31 项契约测试（SQLite 内存库）
npx tsx scripts/e2e-cloud.ts             # 前端云同步端到端（需 uvicorn 已启动）
```

## 五、云存档语义（PRD 3.3）

- 服务端权威版本号：客户端每次写入带 `base_version`，不一致返回 409 并附服务端存档，
  前端整体采纳服务端状态。
- 前端 15 秒节流自动推送，切后台时补推；一切网络失败静默降级为本地模式，游戏不受影响。
- 游客账号按 `device_id` 幂等；换设备 / 清浏览器数据需绑定微信/QQ/邮箱后才能找回
  （绑定接口后端已就绪，绑定 UI 尚未做）。
