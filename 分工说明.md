# 《凡尘问道》开发分工说明

> 生成时间：2026-09-13
> 项目目录：`D:/Downloads/凡尘问道`
> 仓库：https://github.com/Sitozzmonash/XiuXianGame.git
> 产品总纲：`D:/Downloads/修仙挂机推关游戏/凡尘问道_Master_PRD_v3.0.md`（唯一事实源）
> 开发要求：`D:/Downloads/修仙挂机推关游戏/凡尘问道_开发Agent_Prompt.md`

技术栈：Next.js 16（App Router）· React 19 · TypeScript 5.7 · Tailwind CSS v4 · Zustand · Canvas 2D

---

## 一、分工原则

| 角色 | 负责范围 | 一句话界定 |
|---|---|---|
| **前端 / UI（甲）** | `app/` `components/` `app/globals.css` `lib/navigation.ts` | 玩家看得见、点得到的一切：页面、Overlay、动效、样式、渲染 |
| **逻辑 / 数据 / 后端（乙）** | `lib/game/config/` `lib/game/engine/` `lib/game/state/` `server/` `public/images` | 玩家看不见的一切：数值、战斗判定、存档、配置内容、云存档接口 |

**三条铁律**

1. **文件不许越界**。表二里写归谁就归谁，改别人的文件必须先说一声。
2. **`lib/game/types.ts` 是唯一契约**，双方都可读、都不可单方面改。要加字段先在群里说，改完立刻通知对方。
3. **UI 层禁止硬编码游戏数据**（PRD 第 4 条）。关卡、剧情、法宝、装备、NPC 一律从配置/存档读，不许写死在组件里。

---

## 二、文件归属总表

状态：✅ 已完成 ｜ 🔄 进行中 ｜ ⬜ 未开始

### 甲 —— 前端 / UI

| 文件 / 目录 | 行数 | 状态 | 说明 |
|---|---|---|---|
| `app/layout.tsx` | 59 | ✅ | 根布局、字体、元信息 |
| `app/page.tsx` | 9 | ✅ | 挂载 `GameShell` |
| `app/globals.css` | 256 | ✅ | 设计 token：墨/玉青/暗金/宣纸/朱砂 + 工具类 |
| `lib/navigation.ts` | 30 | ✅ | 路由与底部导航定义 |
| `components/game/GameShell.tsx` | 155 | 🔄 | 主外壳与路由分发，**待接入存档** |
| `components/game/primitives.tsx` | 215 | ✅ | `Panel` / `InkButton` / `StatBar` / `QualityBadge` 等原子件 |
| `components/game/GameIcon.tsx` | 124 | ✅ | 图标系统 |
| `components/game/GameHeader.tsx` | 104 | 🔄 | 顶部资源栏，**待接入真实存档** |
| `components/game/BottomNavigation.tsx` | 80 | ✅ | 底部导航 |
| `components/game/SideMenu.tsx` | 53 | ✅ | 左右侧边菜单 |
| `components/game/ChapterHeader.tsx` | 101 | 🔄 | 章节标题，**待接入地图配置** |
| `components/game/GameModal.tsx` | 84 | ✅ | 弹层基座 |
| `components/game/ScreenFrame.tsx` | 58 | ✅ | 页面外壳 |
| `components/game/Tabs.tsx` | 46 | ✅ | 标签页 |
| `components/game/ItemGrid.tsx` | 81 | ✅ | 物品网格 |
| `components/game/EquipmentSlot.tsx` | 49 | ✅ | 装备槽 |
| `components/game/TreasureBars.tsx` | 108 | 🔄 | 5+2 法宝栏，**待接入存档** |
| `components/game/RealmProgress.tsx` | 136 | 🔄 | 境界进度，**待接入境界配置** |
| `components/game/RankItem.tsx` | 63 | ✅ | 排行榜条目 |
| `components/game/GameBackdrop.tsx` | 34 | ✅ | 背景层 |
| `components/game/BattleScene.tsx` | 100 | ⬜ | 旧战斗占位，**待删除或改造** |
| `components/game/screens/` ×12 | 959 | 🔄 | 12 个页面，**待逐个接入存档** |
| `components/game/modals/` ×4 | 270 | 🔄 | 装备 / 法宝 / 挂机 / 失败弹窗，**待接入存档** |
| **`components/game/battle/BattleCanvas.tsx`** | 959 | ✅ | Canvas 水墨战斗渲染（DONE） |
| **`components/game/battle/objects.ts`** | 1631 | ✅ | 对象池 / 粒子 / 弹道 / 光束 / 光柱（DONE） |
| **`components/game/battle/BattleHud.tsx`** | 268 | ✅ | 战斗 HUD：血条、护盾、法宝冷却环（DONE） |
| **`components/game/battle/preview.tsx`** | 167 | ✅ | 特效预览页（临时验收用） |
| **`components/game/overlays/StoryOverlay.tsx`** | 373 | ✅ | 剧情对话：打字机、立绘、历史、选择（DONE） |
| **`components/game/overlays/EncounterOverlay.tsx`** | 163 | ✅ | 奇遇选择（DONE） |
| **`components/game/overlays/SagaBook.tsx`** | 256 | ✅ | 仙途录：章节 / 因果 / 人物（DONE） |
| **`components/game/overlays/RealmRouteOverlay.tsx`** | 466 | ✅ | 秘境路线图（DONE，待接入真实 run） |
| **`components/game/screens/SecretRealmScreen.tsx`** | 296 | ✅ | 秘境承载页（DONE，待注入战斗画面） |
| `components/ui/button.tsx` | 58 | ✅ | shadcn 基座按钮 |

### 乙 —— 逻辑 / 数据 / 后端

| 文件 / 目录 | 行数 | 状态 | 说明 |
|---|---|---|---|
| `lib/game/types.ts` | 745 | ✅ | **契约文件，双方共用，改前必须通知** |
| `lib/game/utils.ts` | 84 | ✅ | 可播种随机、属性合并、格式化 |
| `lib/game/config/realms.ts` | 169 | ✅ | 10 大境界 × 阶段、修为门槛 |
| `lib/game/config/materials.ts` | 68 | ✅ | 40 种材料（炼器 20 + 突破 20） |
| `lib/game/config/treasures.ts` | 447 | ✅ | 30 件法宝 |
| `lib/game/config/techniques.ts` | 355 | ✅ | 24 本功法 |
| `lib/game/config/equipment.ts` | 408 | ✅ | 60 装备模板 + 词条池 + 掉落生成 |
| `lib/game/config/pets.ts` | 241 | ✅ | 15 只灵兽 |
| `lib/game/config/pills.ts` | 315 | ✅ | 30 个丹药 |
| `lib/game/config/story/npcs.ts` | 212 | ✅ | 13 名 NPC |
| `lib/game/config/story/main_story.ts` | 1083 | ✅ | 13 个主线节点（青石村→落霞山脉） |
| `lib/game/config/story/encounters.ts` | 1582 | ✅ | 30 个奇遇 |
| `lib/game/config/story/chapters.ts` | 55 | ✅ | 第 1~4 章 |
| `lib/game/config/story/secret_realms.ts` | 499 | ✅ | 3 个秘境（含落霞古洞府） |
| `lib/game/engine/battle.ts` | 882 | ✅ | 伤害公式、Boss 机制、掉落、`simulate`、`LiveBattle` |
| `lib/game/engine/story.ts` | 374 | ✅ | 条件求值、效果结算 |
| `lib/game/engine/realm.ts` | 272 | ✅ | 秘境路线图与推进 |
| **`lib/game/config/maps.ts`** | — | ⬜ | **地图 / 怪物 / 200 关** ← 当前最关键的缺口 |
| **`lib/game/config/index.ts`** | — | ⬜ | 配置统一出口 |
| **`lib/game/state/store.ts`** | — | ⬜ | **Zustand 存档状态机** ← 当前第二关键缺口 |
| **`lib/game/state/selectors.ts`** | — | ⬜ | 派生数据（战力、属性面板、Build 评分） |
| `lib/game/engine/idle.ts` | — | ⬜ | 24h 离线挂机结算 |
| `lib/game/engine/breakthrough.ts` | — | ⬜ | 境界突破与失败保护 |
| `lib/game/engine/loot.ts` | — | ⬜ | 掉落 → 背包 / 自动分解 / 保底 |
| `server/**` | — | ⬜ | FastAPI：游客登录、云存档、多端同步 |
| `public/images/npc/*.png` | — | ⬜ | NPC 立绘（13 名，每人 5 种情绪） |
| `lib/game-data.ts` | 611 | ⚠️ | **旧原型假数据，UI 已全量依赖**，接入存档后删除 |

---

## 三、关键：三个接口边界

这是甲乙双方唯一需要对齐的东西。

### 边界 1 —— 存档形状（`lib/game/types.ts` 的 `GameSave`）

UI 只读这个结构，逻辑层只写这个结构。当前已定义的分支：

```
profile    角色名 / 境界阶段 / 修为 / 灵根 / 流派
resources  灵石 / 仙玉 / 宗门贡献
progress   全局关卡 / 当前地图 / 稳定刷怪点 / 已通关秘境
combat     10 装备槽 / 5 主动法宝 / 2 被动法宝 / 功法 / 灵兽 / 备用方案
inventory  装备列表 / 材料 / 丹药 / 容量
story      flags / 已看节点 / 待播放队列 / 奇遇历史
npcs       NPC 关系值 / 状态 / 存活
karma      9 项因果变量
realmRun   当前秘境进度（可空）
idle       上次领取时间
stats      击杀数 / 精英 / Boss / 死亡 / 奇遇数 / 游戏时长
log        玩家日志
settings   自动战斗 / 速度 / 音效 / 画质档位
```

### 边界 2 —— 甲对乙的请求清单（乙按此实现 store API）

下面这些是 UI 需要的动作。名字可调，**语义不可少**：

| UI 场景 | 需要的动作 |
|---|---|
| 顶栏资源 | `save.resources.stone` / `resources.immortalJade` / `profile.cultivation` |
| 点击「战斗」 | `startBattle()` → 返回 `LiveBattle` 实例供 `BattleCanvas` 消费 |
| 战斗结束 | `finishBattle(win)` → 结算掉落、修为、灵石，写入 `log` |
| Boss 失败 | `retreatToStable()` → 回到稳定刷怪点，返回 `failReason` |
| 领取挂机 | `claimIdle(now)` → 返回 `{ cultivation, stone, drops, duration }` |
| 装备操作 | `equip(uid)` / `unequip(slot)` / `salvage(uid)` / `autoEquip()` / `lockItem(uid, v)` |
| 法宝操作 | `equipTreasure(slotIndex, defId)` / `upgradeTreasure(defId)` |
| 功法操作 | `setMainTechnique(id)` / `setSupportTechnique(i, id)` |
| 突破 | `canBreakthrough()` / `doBreakthrough()` → 返回成功与否与新境界 |
| 剧情 | `pendingStory` 队列 + `submitChoice(nodeId, choiceId)` |
| 奇遇 | `rollEncounter()` → 返回 `StoryNode` 或 null |
| 秘境 | `enterRealm(id)` / `moveToNode(id)` / `leaveRealm()` |
| 设置 | `setAuto(v)` / `setSpeed(n)` / `setQuality(q)` |
| 存档 | `reset()` / `exportSave()` / `importSave(json)` |

### 边界 3 —— 甲对乙的渲染输入（乙必须产出这些）

```ts
// 战斗画面
<BattleCanvas
  events={BattleEvent[]}        // 每帧新增事件（LiveBattle.tick 的返回值）
  state={LiveBattleState}       // 实时快照：双方血量、护盾、技能冷却、DPS
  enemy={{ name, image, icon, isBoss }}
  player={{ name, image }}
  width height quality finished  // 尺寸与画质档位
/>

// 剧情
<StoryOverlay node={StoryNode} getChoiceState={(id) => ({ enabled, reason })} onChoose={...} />

// 秘境
<SecretRealmScreen node={RealmNode} run={RealmRun} battleSlot={<BattleCanvas ... />} />
```

---

## 四、甲（前端 / UI）的工作清单

按依赖顺序，前 5 项是打通闭环的必经之路。

| # | 任务 | 涉及文件 | 验收标准 |
|---|---|---|---|
| F1 | **接入存档**：顶栏、章节头、法宝栏、境界进度改读存档 | `GameHeader` `ChapterHeader` `TreasureBars` `RealmProgress` | 数值随存档变化，刷新不丢 |
| F2 | **页面接入**：12 个 screen 全部改读存档 + 触发 store 动作 | `components/game/screens/*` | 每个页面显示真实数据，操作有反馈 |
| F3 | **战斗接入**：BattleScreen 换成 `BattleCanvas` + `BattleHud` | `screens/BattleScreen.tsx` `battle/*` | 手机竖屏 30~60FPS，技能特效可见，爆装光柱出现 |
| F4 | **剧情接入**：推关遇 `storyId` 时弹 `StoryOverlay`，选择写回 | `GameShell` `overlays/StoryOverlay` | 选择后 flag 变化，后续对话不同 |
| F5 | **奇遇接入**：每 5~15 分钟一次，`EncounterOverlay` 承载 | `GameShell` `overlays/EncounterOverlay` | 奇遇有冷却、有历史、结果可追踪 |
| F6 | **秘境接入**：入口 → 路线图 → 节点 → 结算 | `SecretRealmScreen` `RealmRouteOverlay` | 完整跑通落霞古洞府，分支路线可走 |
| F7 | **仙途录入口**：主界面加入口，展示章节/因果/人物 | `overlays/SagaBook` `SideMenu` | 三页可切换，数据来自存档 |
| F8 | **突破演出**：水墨扩散 + 境界文字 + 天雷/心魔差异 | 新组件 `overlays/BreakthroughOverlay` | PRD 第 5、37 章的演出要求 |
| F9 | **新手指引**：1~10 关逐步解锁提示，不一次塞满 | `GameShell` + 新组件 | PRD 第 34 章解锁节奏 |
| F10 | **手机适配**：安全区、刘海、触摸目标 ≥ 40px | `globals.css` + 各页面 | iPhone 竖屏无裁切、无误触 |
| F11 | **画质档位**：低/中/高切换联动粒子数与特效 | `BattleCanvas` + 设置页 | 低端机稳定 30FPS |
| F12 | **音效接入**：飞剑/雷击/爆装/突破/UI | 新 `lib/audio.ts` + Howler 或 WebAudio | 可关闭，不阻塞首屏 |
| F13 | **清理**：删掉 `lib/game-data.ts` 与所有硬编码残留 | 全 `components/` | 全局搜索无 `game-data` 引用 |

---

## 五、乙（逻辑 / 数据 / 后端）的工作清单

| # | 任务 | 涉及文件 | 交付物 |
|---|---|---|---|
| B1 | **地图与关卡**（最高优先，挡住所有人） | `lib/game/config/maps.ts` | 4 张图 × 50 关 = 200 关；24~30 个怪物含 Boss 机制；导出 `MONSTERS` `MAPS` `MONSTER_BY_ID` `getStage()` `monsterStats()` `globalToMap()` `stageNumberToGlobal()`。第 1 图第 3/10/25/50 关挂 `qs_01_missing`/`qs_02_jade`/`qs_03_shrine`/`qs_04_depart`；第 2 图第 5/30/50 关挂 `hf_01_shen`/`hf_02_luoqi`/`hf_03_blackwind`；第 3 图第 10/40/50 关挂 `yz_01_market`/`yz_02_ali`/`yz_03_auction`；第 4 图第 20/45/50 关挂 `lx_01_ruins`/`lx_02_stele`/`lx_03_xiewuchen`；第 1 图第 45 关挂 `realmId: realm_qingshi_trial`，第 2 图第 45 关挂 `realm_heifeng_cave`，第 4 图第 50 关挂 `realm_luoxia_cave` |
| B2 | **配置统一出口** | `lib/game/config/index.ts` | re-export 全部配置模块，前端单点引用 |
| B3 | **Zustand 存档状态机**（第二优先） | `lib/game/state/store.ts` | 实现第三节边界 2 的全部动作；`persist` 中间件写 localStorage；`version` 号 + 迁移函数 |
| B4 | **派生数据** | `lib/game/state/selectors.ts` | 战力估算、属性面板、Build 评分（PRD 第 44 章：战力≠胜负唯一指标） |
| B5 | **离线挂机** | `lib/game/engine/idle.ts` | 24h 上限，公式 = 关卡基础 × 境界 × 洞府 × 功法 × 离线时长 |
| B6 | **境界突破** | `lib/game/engine/breakthrough.ts` | 修为上限 + 材料 + Boss/心魔/天劫；失败不掉境界、不掉装备、修为不清零 |
| B7 | **掉落与背包** | `lib/game/engine/loot.ts` | 自动分解、词条保护、锁定、容量扩容；前 10 分钟必见蓝装、30 分钟内至少一次紫装（PRD 第 45 章） |
| B8 | **扫荡** | 扩展 `engine/battle.ts` | 不渲染战斗，直接出耗时/收益/掉落；高难 Boss 首次禁止扫荡 |
| B9 | **NPC 立绘** | `public/images/npc/*.png` | 13 名 NPC，每人 normal / hurt / angry / happy / cold 五态；命名 `<npcId>.png`、`<npcId>-hurt.png` 等，端口与 `npcs.ts` 的 `portrait` 字段对齐 |
| B10 | **地图背景与 Boss 立绘** | `public/images/` | 4 张地图背景 + 4 个章节 Boss 立绘 + 玩家立绘升级 |
| B11 | **音效素材** | `public/audio/` | 飞剑 / 雷击 / 火焰 / Boss / 红装掉落 / 突破 / 炼丹 / 炼器 / UI / 剧情选择 |
| B12 | **云存档后端** | `server/**` | FastAPI + PostgreSQL：`POST /auth/guest`、`GET /save`、`POST /save`；客户端提交操作、服务端算结果（PRD 第 42、43 章） |
| B13 | **多端登录** | `server/auth/**` | 游客 / 微信 / QQ / 邮箱；游客可绑定不丢档 |
| B14 | **埋点** | `server/analytics/**` | PRD 第 49 章的 13 项基础埋点 + 7 项核心观察 |

---

## 六、依赖关系

```
B1 地图配置 ─────┬──> B3 存档状态机 ──> F1 接入存档 ──> F2 页面接入
                 │                                    │
B2 配置出口 ─────┘                                    ├──> F4 剧情接入
                                                      ├──> F5 奇遇接入
B5 挂机 ────────> B3                                  └──> F6 秘境接入
B6 突破 ────────> B3                                       │
B7 掉落 ────────> B3                                       └──> F8 突破演出
B8 扫荡 ────────> F2
B9/B10 美术 ────> F2 / F4 / F6（立绘与背景）
```

**当前卡点**：`B1` 地图配置未完成 → `B3` 存档状态机无法启动 → `F1` 之后的全部前端工作都在等。建议乙先只做 B1，做完立刻通知。

**甲乙可并行、互不阻塞的部分**：
- 甲：`F3` 战斗接入（引擎已就绪）、`F8` 突破演出、`F10` 手机适配、`F12` 音效、`F13` 清理
- 乙：`B9` `B10` 美术素材、`B12` `B13` 后端接口

---

## 七、验收标准

### 甲（前端 / UI）

- [ ] 手机竖屏（375×812 起）无裁切、无横向滚动、触摸目标 ≥ 40px
- [ ] 战斗画面在手机端稳定 ≥ 30FPS，PC ≥ 60FPS
- [ ] 全站无 `lib/game-data.ts` 引用，无硬编码关卡/剧情/数值
- [ ] 所有弹层可关闭、可返回，无死路
- [ ] 立绘/背景加载失败时优雅降级，绝不出现破图
- [ ] 剧情 Overlay：打字机、跳过、自动、历史记录、选择按钮齐全（PRD 第 33 章）
- [ ] 秘境路线图：分支可见、可选节点高亮、已走过路径可追溯
- [ ] 红装掉落有冲天光柱 + 轻震 + 独立音效；彩装有五色异象 + 停顿（PRD 第 19 章）

### 乙（逻辑 / 数据 / 后端）

- [ ] 200 关逐关可查，关卡节奏符合 PRD 第 11 章，无连续 20 关纯加血
- [ ] `simulate()` 与 `LiveBattle` 结果一致（同一关卡同一 Build，胜率偏差 < 5%）
- [ ] 存档刷新不丢；`version` 升级有迁移；导出/导入可用
- [ ] 24h 离线上限正确；离线不替玩家做重大选择
- [ ] 突破失败不掉境界、不掉装备、修为不清零
- [ ] 前 10 分钟必见蓝装；30 分钟内至少一次紫装
- [ ] 每个 Boss 至少 2~3 种合理应对方式，不存在唯一解（PRD 第 14 章）
- [ ] 剧情 flag 前后可重放验证；NPC 死亡后不得无条件出现；唯一物品不可重复获得（PRD 第 50 章）

---

## 八、协作规则

1. **改动 `lib/game/types.ts`**：先说明要加什么字段、为什么，另一方确认后再改，改完通知。
2. **文件冲突**：各自只改自己名下的文件。需要对方文件配合时，在群里说清楚「哪个文件、什么函数、什么签名」。
3. **提交习惯**：小步提交，提交信息写清「改了什么 / 验证了什么 / 还剩什么」（PRD 第 55 章的汇报格式）。
4. **验收方式**：甲每完成一个功能必须在浏览器实际跑通手机竖屏；乙每完成一个系统必须跑模拟或写校验脚本。
5. **禁止**：未经沟通重构对方模块；一次性生成 1000 关静态代码；把内容写死在 UI；删掉别人未提交的工作。
6. **PRD 优先级**：文档冲突时以 `凡尘问道_Master_PRD_v3.0.md` 为准。

---

## 九、当前可验收的东西

```bash
cd D:/Downloads/凡尘问道
npm install       # 已完成
npm run dev       # 启动开发服务器
```

| 地址 | 内容 |
|---|---|
| `/` | **完整可玩**：登录 → 主界面 → 战斗 → 剧情 / 奇遇 → 装备 / 法宝 / 功法 / 秘境 / 仙途录 / 突破 |
| `/battle-preview` | **战斗特效与音效试听**：全类型特效循环播放，可切画质档位与逐个试听音效 |

已完成的验证（无头全流程）：**1→200 关全部走通**，15 次境界突破，3 个秘境全部通关，
12 个剧情节点在正确关卡触发，到手 14 件法宝 / 17 本功法 / 1 只灵兽，
主修与 3 个辅助功法槽自动装备，9 项因果变量正常累积。

### 尚未开放的功能（配置与状态层都还没有对应数据）

洞府、宗门、排行榜在 PRD 里属于 Phase 5「长期养成」，`lib/game/config/` 与
`lib/game/state/store.ts` 目前都没有对应实现。这三个入口已改为**如实告知的
开发计划页**（`components/game/screens/ComingSoon.tsx`），并展示已经真实生效的
部分（洞府倍率、宗门声望、玩家自己的成绩），不再显示假数字。

要真正开放它们，需要先补：

| 功能 | 需要补的东西 |
|---|---|
| 洞府 | 设施配置（8 个设施的等级与效果）+ 存档字段 + 升级动作；洞府倍率已由 `caveFactor` 读取 flag |
| 宗门 | 宗门配置 + 存档里的宗门归属 / 贡献 / 任务进度 + 宗门任务与商店 |
| 排行榜 | 服务端快照接口（`server/`）+ 客户端榜单读取；本地成绩已可展示 |

---

## 十、一句话总结

**甲（前端）**：把已经写好的空壳界面，接上真实存档、战斗画面、剧情与秘境，做出一个手机上能流畅玩、看起来是国风水墨修仙的画面层。

**乙（逻辑）**：先把 `lib/game/config/maps.ts` 的 200 关和 `lib/game/state/store.ts` 的存档状态机做出来——这两件事做完，前端才能活过来；然后补齐挂机、突破、掉落、素材与后端。
